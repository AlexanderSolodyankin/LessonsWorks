package HomeWorks.homeWorks27;

 class ReadMe {
  /**
   * 1. Как вы понимаете понятие полиморфизма?
   * принятие любой формы или безформенный
   * в данном случае это класс или метод который используется как ссылка для работы с обьектами
   * то есть что то безформенное которое может повторить форму чегото
   *
   * 2. Что такое кастинг(cast) типов в мире программирования?
   * Это указание в какой тип должно быть преабразованно или
   * какой тип должен быть вложен если он находится в общей куче разных типов
   *
   * 3. Зачем мы храним объекты типа Shark, Duck, Turtle в одном массиве?
   * для удобства их использования без моногамного испольщщования а для обобщения
   * так как они имеют какой то общий параметр
   *
   * 4. Какие есть ограничения при хранении разных типов (Shark, Duck, Turtle и т.д.) в одном массиве (Swimmable)?
   * их нельзя переложить без явного преоброзования так же при проверке в случае если данный тип пустой
   *
   * 5. Что произойдет после того как отработает следующая строчка кода, опишите подробнее: Person[] people;
   *  будет созданная незакрепленная ссылка которая отсылается в пустоту но готовая принять обьект в виде массива
   *  если его определить
   */
 }
